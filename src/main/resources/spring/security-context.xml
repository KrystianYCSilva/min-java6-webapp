<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <http auto-config="false" use-expressions="true" create-session="ifRequired">
        <intercept-url pattern="/api/relatorios/**" access="isAuthenticated()"/>
        <intercept-url pattern="/api/**" access="permitAll"/>
        <http-basic/>
        <csrf disabled="true"/>
        <custom-filter ref="sessionUsuarioAuthenticationFilter" before="BASIC_AUTH_FILTER"/>
    </http>

    <authentication-manager alias="authenticationManager">
        <authentication-provider ref="usuarioAuthenticationProvider"/>
    </authentication-manager>

    <beans:bean id="sessionUsuarioAuthenticationFilter"
                class="br.gov.inep.censo.spring.security.SessionUsuarioAuthenticationFilter"/>

    <beans:bean id="usuarioAuthenticationProvider"
                class="br.gov.inep.censo.spring.security.UsuarioAuthenticationProvider">
        <beans:property name="usuarioRepository" ref="usuarioRepository"/>
    </beans:bean>
</beans:beans>
